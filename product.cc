#include <iostream>
#include <string>
using namespace std;

int ans;
int s[3];
int main(){

for(i=0;i<3;i++){
    cin >> s;
    if(s[i]==1) ans++;
   // if(s[1]==1) ans++;
   // if(s[2]==1) ans++;
}
    printf("%d", ans);

}